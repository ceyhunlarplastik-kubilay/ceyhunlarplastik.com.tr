/**
 * Gruplanmış tablo satırı — uygulama CORE'dadır (gruplama artık sunucuda yapılıyor).
 *
 * Bu dosya yalnız mevcut import yollarını ve tip adını korur; kural burada
 * YENİDEN YAZILMAZ (bkz. core/helpers/products/groupVariantTableRows.ts).
 */
export type { GroupedVariantRow as GroupedMeasurementOption } from "@core/helpers/products/groupVariantTableRows"
